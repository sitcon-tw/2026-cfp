---
import StarIcon from "@assets/desc_page/star.svg";

import { getImage } from "astro:assets";

import BannerBackground from "@assets/desc_page/banner_bg.webp";
const optimizedBannerBackground = await getImage({ src: BannerBackground, format: "png" });
---

<div class="banner" style={`background-image: url(${optimizedBannerBackground.src});`}>
	<h3 class="banner-text">
		那段未曾被聽見的旋律，等著在混沌中覺醒。
		<br />
		現在是你的使命，投下專屬於你的作品，為失序添上一抹即興，喚起共鳴。
	</h3>
	<h2 class="banner-title">別再猶豫，讓樂音在這場年會中響起！</h2>

	<div class="banner-button">
		<StarIcon height="2rem" />
		<span> 立即投稿 </span>
		<StarIcon height="2rem" />
	</div>
</div>

<style>
	.banner {
		padding: 16rem 4rem 8rem 4rem;
		width: 100%;
		background-repeat: no-repeat;
		background-position: center;
		background-size: cover;
	}

	.banner-text {
		display: inline-block;
		font-size: 2rem;
		max-width: 100%;
		text-wrap: balance;
		line-height: 5rem;
		padding: 2rem 0;
		background: linear-gradient(90deg, #2aa6ff, #009dff, #f8981d, #e84b1c);
		background-clip: text;
		-webkit-text-fill-color: transparent;
		color: transparent;
	}

	.banner-title {
		font-size: 5rem;
		padding: 2rem 0;
		background: linear-gradient(90deg, #2aa6ff, #009dff, #f8981d, #e84b1c);
		background-clip: text;
		-webkit-text-fill-color: transparent;
		color: transparent;
		text-wrap: balance;
	}

	.banner-button {
		display: flex;
		flex-wrap: wrap;
		border-radius: 6.25rem;
		background: #010121;
		width: 100%;
		font-size: 2rem;
		line-height: 4.5rem;
		font-weight: bold;
		align-items: center;
		justify-content: center;
		margin: 4.5rem 0;
	}
</style>

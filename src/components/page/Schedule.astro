---
const { schedule } = Astro.props;

const diffDate = (a: Date) => a.getTime() - new Date().getTime();
---

<div class="schedule">
	<h2>重要時程</h2>
	<div class="divider"></div>
	{
		schedule.map((item: { date: Date; event: string }) => (
			<div class={`event ${diffDate(item.date) > 0 ? "event-before" : ""}`}>
				<div class="event-col">{item.date.toLocaleDateString("zh-Hant")}</div>
				<div class="event-col">{item.event}</div>
			</div>
		))
	}
</div>

<style>
	.schedule {
		max-width: 15rem;
		text-align: center;
	}

	.divider {
		height: 0.25rem;
		width: 100%;
		background: #ffffff;
	}

	.event {
		display: flex;
		border-radius: 1rem;
		background: #8d5657;
		width: 100%;
		align-content: center;
		height: 2rem;
		margin-top: 1rem;
		align-items: center;
	}

	.event-before {
		background: #ffffff;
		color: #000000;
	}

	.event-col {
		flex: 1;
	}
</style>
